// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://astrophysics108.github.io',
    base: '/daydream-participant-guide',
});
